var canvas = document.getElementById('canvas'),    context = canvas.getContext('2d');// Now setting the width and height of the canvascanvas.width = $(window).width() * 0.7;canvas.height = $(window).height() * 0.9;var avg = (canvas.width/canvas.height + canvas.height/canvas.width) / 2;$(window).on('resize', function(){  canvas.width = $(window).width() * 0.7;  canvas.height = $(window).height() * 0.9;  avg = (canvas.width/canvas.height + canvas.height/canvas.width) / 2;});